package com.online.ocCommon.storage;

import com.qiniu.util.Auth;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * 七牛SDK的包装类，以便于业务使用
 */
public class QiniuWrapper {
    private static final Logger log = LoggerFactory.getLogger(QiniuWrapper.class);

    private static final Auth auth = null;
}
